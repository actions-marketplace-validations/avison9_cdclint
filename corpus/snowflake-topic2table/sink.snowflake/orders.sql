CREATE OR REPLACE TABLE ANALYTICS.CDC.ORDERS (
    ID              VARCHAR,
    CUSTOMER_ID     VARCHAR,
    TOTAL_CENTS     NUMBER(38, 0),
    CURRENCY        VARCHAR(3),
    CREATED_AT      TIMESTAMP_TZ,
    RECORD_METADATA VARIANT
);
